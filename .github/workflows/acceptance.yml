name: acceptance
on:
  pull_request:
    branches: [master]
permissions:
  contents: read
jobs:
  # The suite itself lives in acceptance-suite.yml, shared with nightly.yml so
  # the two can never drift apart.
  acceptance:
    uses: ./.github/workflows/acceptance-suite.yml
